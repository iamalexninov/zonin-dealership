import { BottomNavbar } from "./bottom/BottomNavbar";
import { TopNavbar } from "./top/TopNavbar";

export const Navbar = () => {
  return (
    <header>
      <TopNavbar />
      <BottomNavbar />
    </header>
  );
};
